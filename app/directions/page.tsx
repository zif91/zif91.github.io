import { Cta } from '../components/Cta'; import { Breadcrumbs, Footer, Header } from '../components/Chrome'; import { directions } from '../lib/content';
export const metadata = { title: 'С чем помогаем — Гармония', description: 'Направления психологической помощи в клинике Гармония.' };
export default function DirectionsPage(){return <><Header/><main className="inner-main"><Breadcrumbs items={[{label:'С чем помогаем'}]}/><section className="inner-hero"><span className="section-kicker">Направления</span><h1>Начните с того,<br/><em>что беспокоит сейчас</em></h1><p>Не обязательно знать диагноз или метод терапии. Выберите ситуацию, которая звучит знакомо.</p></section><section className="direction-list">{directions.map((item,index)=><a href={`/directions/${item.slug}`} className={item.color} key={item.slug}><span>0{index+1}</span><div><h2>{item.title}</h2><p>{item.lead}</p></div><b>Подробнее ↗</b></a>)}</section><Cta/></main><Footer/></>}
